import React,{useState} from 'react'
export default function ProfileModal({state,setState,onClose}){const [name,setName]=useState(state.name||'');return <div className="modal-back"><div className="modal"><button className="close" onClick={onClose}>×</button><span className="kicker">Профиль</span><h2>Как тебя<br/><em>называть?</em></h2><input value={name} maxLength={24} onChange={e=>setName(e.target.value)} placeholder="Имя или ник" autoFocus/><button className="primary" onClick={()=>{setState({...state,name:name.trim()});onClose()}}>Сохранить</button><p>Имя хранится только на этом устройстве.</p></div></div>}
